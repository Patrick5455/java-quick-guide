package java101;

public class NamingConventions {
	
	public static void main(String[] args) {
		
		// naming id very important in programming
		// it is important you variable names are descriptive enough
		
		/* The Following are what can be named in java:
		 * 
		 * -Package : this should be all lower case
		 * -Interface : this should start with Capital Letter
		 * -Class: this should start with Capital letter
		 * -Methods: this should be in lower case
		 * -Variables: this should be in lower case
		 * -Constants: this should be all CAPITAL LETTERS
		 * - 
		 * */
		
		
		// Camel Case Rule in Java:
		
		/* when combining two words or more as names in java
		 * you join them together with the next word starting with a capital letter
		 * e.g: 
		 * 		NewClass
		 * 		newMethod
		 * 		newPackage
		 * 		NewInterface
		 * 
		 * However, this does not apply to constant or ENUM  types
		 * since they are written in UPPERCASE all through, we are separated by underscores
		 * e.g THIS_IS_ENUM 
		 * 
		 * 
		 * */
		
		// It is important to 
		
	}

}
